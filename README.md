# mobile_UTG_annotate_tool
This project is for developers to collect mobile UI transition graph data
1) run the mqtt_client_human.py to start the mqtt service of the backend
2) run the UTG_constructor on Android devices to collect UI transition graph of target apps
